rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false; // Default to secure
    }

    // Allow the server-side Cloud Function to bypass these rules
    // (Firebase Admin SDK bypasses security rules)
  }
}
